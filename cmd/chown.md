# chown

> chown 命令 ‌ 是 Linux/Unix 系统中用于修改文件或目录所有者（owner）和所属组（group）的核心命令，基本语法为 chown [选项] 用户[:组] 文件，需 root 权限执行。

-R 递归修改目录及其子内容的所有权

在 chmod 命令中，777 表示为所有用户（所有者、所属组、其他人）开放以下权限：

‌ 读（r）‌：数值为 4
‌ 写（w）‌：数值为 2
‌ 执行（x）‌：数值为 1
